import clsx from 'clsx'

const styles = {
  wrapper: clsx('mt-8 flex items-center'),
  icon_container: clsx('mr-[1.6rem]'),
  icon: clsx('h-[2.4rem] w-[2.4rem] fill-primary'),
  detail: clsx('text-left'),
  paragraph: clsx('text-[1.6rem] first:text-3xl first:font-semibold'),
}

export default styles
