import clsx from 'clsx'

const styles = {
  wrapper: clsx('flex w-full flex-col items-center justify-around bg-black p-12 md:flex-row'),
  links: clsx('flex flex-row'),
  title: clsx('mb-3 text-[4rem] font-semibold text-white md:mb-0'),
}

export default styles
