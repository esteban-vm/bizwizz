import clsx from 'clsx'

const styles = clsx(
  'ml-10 block select-none pb-[0.8rem] ',
  'text-[1.4rem] uppercase text-white transition-[border-b]',
  'hover:border-b-2 hover:border-b-primary'
)
export default styles
