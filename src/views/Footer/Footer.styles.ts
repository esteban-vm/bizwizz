import clsx from 'clsx'

const styles = {
  wrapper: clsx('flex justify-between p-[3rem_0]'),
  copyright: clsx('text-darkgrey'),
  links: clsx('flex'),
  author: clsx('text-white'),
}

export default styles
