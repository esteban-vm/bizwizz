import clsx from 'clsx'

const styles = {
  wrapper: clsx('flex justify-evenly p-[3rem_0] text-xl'),
  copyright: clsx('text-darkgrey'),
  links: clsx('flex flex-row'),
  author: clsx('text-white hover:opacity-80'),
}

export default styles
