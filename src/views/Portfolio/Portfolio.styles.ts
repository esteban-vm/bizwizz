import clsx from 'clsx'
import { commonStyles } from '@/styles'

const styles = {
  ...commonStyles,
  works: clsx('flex items-center justify-center gap-3'),
  work_column: clsx('basis-1/5'),
}

export default styles
