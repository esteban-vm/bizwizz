import clsx from 'clsx'

const styles = {
  wrapper: clsx('flex flex-wrap items-center justify-center gap-1'),
  column: clsx('flex basis-2/5 flex-col gap-1 md:basis-1/3 lg:basis-1/5'),
}

export default styles
